<div style="position: relative">
  <div style="margin-left:60px">
    <p><strong><%= @forum_post.user.name %></strong> <small>commented:</small></p>
    <%= @forum_post.text.body.html_safe %>
  </div>
</div>

<br />


    
<p><%= link_to "Reply", forum_thread_url(@forum_post.forum_thread, page: ((@forum_post.forum_thread.forum_posts.order(:created_at).index(@forum_post) / ForumPost.per_page) + 1), anchor: "forum_post_#{@forum_post.id}"), style: "background:#be2126; color:#fff; text-decoration:none; padding: 10px 20px" %></p>
